# Parkster API auth (Basic auth header value)
PARKSTER_AUTH=Basic <base64-encoded-credentials>

# EasyPark JWT token (refresh via mitmproxy capture)
# Store at /tmp/easypark_token.txt or set here
# EASYPARK_TOKEN=<jwt-token>
